version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: anticristiancpu-pinakes_web_1
      APP_PORT: 80

  web:
    image: jbenamy/pinakes-docker:latest
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data:/var/www/html