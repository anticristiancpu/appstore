version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: anticristiancpu-ownfoil_web_1
      APP_PORT: 8465

  web:
    image: a1ex4/ownfoil:latest
    restart: on-failure
    environment:
      - PUID=1000
      - PGID=1000
volumes:
      - ${APP_DATA_DIR}/data/config:/app/config
      - /home/umbrel/umbrel/home/Downloads/switch:/games
